using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PropVisualLevel
{
    Field,
    Shop
}

public class VisualPropsHandler : MonoBehaviour
{
    public PropVisualLevel propVisualLevel = PropVisualLevel.Field;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
